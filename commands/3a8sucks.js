const discord = require("discord.js");

module.exports.run = async (bot, message, args) => {

  return message.channel.send('mission failed, we get em next time.');

};

module.exports.help = {
  name: "3a8stupid"
};
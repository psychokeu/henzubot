const discord = require("discord.js");

module.exports.run = async (bot, message, args) => {

  return message.channel.send('bam bam with the bam bam with the witch to the top and the pop dont stop');

};

module.exports.help = {
  name: "dancetillyoudrop"
};